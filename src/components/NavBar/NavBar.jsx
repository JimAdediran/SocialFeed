import React from "react";

const NavBar = (props)=>{
    return(
        <header>
            <div>
                <p>Social Feed</p>
            </div>
        </header>
    )
}

export default NavBar